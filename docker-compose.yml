version: '3.8'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: frontend
    container_name: frontend
    restart: always
    ports:
      - '8000:80'
    network_mode: 'host'
# switch to public 80 on vps server
