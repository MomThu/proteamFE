const HOME = '/';
const LOGIN = '/login';
const GOOGLE_LOGIN = '/google-login';
const NOT_FOUND = '/404';
const PROFILE = '/profile';

const routesMap = {
  HOME,
  LOGIN,
  NOT_FOUND,
  GOOGLE_LOGIN,
  PROFILE,
};

export default routesMap;
